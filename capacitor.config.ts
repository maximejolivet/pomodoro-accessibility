import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maximejolivet.pomodorotdah',
  appName: 'Pomodoro TDAH',
  webDir: 'www'
};

export default config;
